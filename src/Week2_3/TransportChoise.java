/*package Week2_3;

public class TransportChoise {

    private final Transport sedan;

    //Initialize for each transport
    private TransportChoise initializeTransport(){
        sedan = new Transport(5,40,8);
    }

    public static void main(String[] args) {

        TransportChoise transportChoise = new TransportChoise();
        TransportChoise.execute();
    }

    private void execute(){
        int distance = 600;
        int person=200;
        int sedanFuelVolume = calculateTotalFuelVolume(distance, person, sedan)
    }

    private int calculateTotalFuelVolume (int distance, int person, Transport transport) {
        return
    }
}*/


/*
import java.util.Scanner;


        Scanner SC = new Scanner(System.in);

        System.out.print("Enter number of person that requested transport: ");
        int n = SC.nextInt();

        System.out.print("Enter distance: ");
        double d = SC.nextDouble();

        distributePassengersPerTransport(distance, person);
    }


    public static void distributePassengersPerTransport(int numberPerson, double distance) {

        Transport bus = new Transport(40, 20, "bus");
        Transport microbus = new Transport(25, 17, "microbus");
        Transport sedan = new Transport(5, 5, "sedan");
        Transport[] choise = {bus, microbus, sedan};

        // Calculate for capacity more than bus places.
    }
}
*/
